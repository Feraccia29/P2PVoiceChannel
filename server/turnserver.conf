# Porta di ascolto
listening-port=16430

# Ascolta su tutte le interfacce
listening-ip=0.0.0.0

# Range porte per relay media (UDP)
min-port=16450
max-port=16500

# Realm
realm=voip-p2p

# Credenziali statiche
user=voipuser:voippass123

# Fingerprint per compatibilità
fingerprint

# Abilita long-term credentials
lt-cred-mech

# Log livello verbose (rimuovi in produzione)
verbose

# No TLS per ora
no-tls
no-dtls
